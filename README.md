# Macro Sage brief history

This orphan branch is the durable, append-only hosted brief-history backend.
It contains structured brief records and document identifiers, never article
bodies, podcast transcripts, private manifests, or credentials.

The format and recovery contract are documented on `main` in
`docs/HISTORY.md`. Application workflows are the only expected writers.

# Copy this file to .env, replace the placeholder, and load it into your shell.
# Never commit a real credential.
OPENAI_API_KEY=replace-with-your-openai-api-key

# Optional runtime overrides used by the prototype entry point.
OPENAI_MODEL=gpt-4.1-mini
